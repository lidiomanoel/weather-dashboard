// Simulação de dados de clima para o teste inicial
document.addEventListener('DOMContentLoaded', () => {
    console.log("Weather Dashboard carregado!");
    // Aqui poderíamos adicionar uma chamada real para a API do OpenWeatherMap futuramente
});